# 优雅适配 DeepSeek-V2-Lite 到 micro-vllm

## 目标
建立 `models/` 架构目录，按 `config.model_type` 分派到 qwen / deepseek 两个适配器。适配后两个模型都能正常加载、推理、服务。保持 Qwen 路径完全不变（行为零回归）。

## 现状诊断（核心耦合点）
当前框架硬编码 Qwen-1 结构，耦合点集中在 3 个文件：

1. **`core/layer/model_graph.py`** (decode)
   - `prepare()`: 从 `block.attn.c_attn` 取融合 QKV、`block.mlp.w1/w2/c_proj` 取 MLP、`block.attn.c_proj` 取 O，切分后存成 `_qkv_w/_qkv_b/_o/_gu/_d`
   - `_compute_qkv`: `rmsnorm_(h, block.ln_1.weight)` → `matmul_v3` QKV
   - `_flash_attn`: `qkv.reshape(bs,3,num_heads,head_size).unbind(1)` → `flash_attn_with_kvcache`（RoPE 由 flash 内部用 `_cos_pool/_sin_pool` 对**整个 head_size** 旋转）
   - `_compute_ffn`: `rmsnorm_residual` ln_2 → SwiGLU MLP
   - `decode()`: `model.transformer.wte / h / ln_f / lm_head`

2. **`core/layer/model_prefill.py`** (prefill) — 同样的 Qwen 结构假设 + RoPE 对整个 head_size

3. **`core/engine.py`** `_init_config` — 从 `config` 读 `num_heads/kv_num_heads/head_size/intermediate_size`，直接喂给 `KVCacheManager` 与两个 runner。

4. **`core/cache_manager.py`** — KV cache 形状 `[n_blocks, block_size, n_heads, head_size]`，存**展开后的 per-head K/V**。

## DeepSeek-V2-Lite 架构关键差异
- **MLA 注意力**：KV cache 必须存**压缩 latent** `[kv_lora_rank(512) + qk_rope_head_dim(64)] = 576`，不是展开的 K/V。attention 时再 `kv_b_proj` 展开。
  - q_proj: hidden→`num_heads*q_head_dim(192)`；q_head_dim = qk_nope(128)+qk_rope(64)
  - kv_a_proj_with_mqa: hidden→`kv_lora_rank(512)+qk_rope(64)=576`
  - kv_a_layernorm: RMSNorm on 512
  - kv_b_proj: 512→`num_heads*(qk_nope+v_head)=16*(128+128)=4096`
  - o_proj: `num_heads*v_head(128)=2048`→hidden
  - RoPE **只**作用于 qk_rope 部分（q_pe/k_pe，各 64 维）
  - softmax_scale = `q_head_dim**-0.5 * mscale^2`（yarn mscale_all_dim=0.707，factor=40）
- **MoE FFN**：layer 0 是 dense MLP；layer 1~26 是 MoE。
  - gate: hidden→64 experts，softmax，top-6（greedy，n_group=1 → 走 `torch.topk` 简单分支）
  - routed_scaling_factor=1.0，norm_topk_prob=false → topk_weight 不归一化（直接 ×scaling=1.0）
  - 64 个 expert MLP（`moe_intermediate_size=1408`，gate/up/down）+ **2 个 shared expert**（合并 intermediate=`1408*2=2816` 的单个 MLP）
  - 推理：`moe_infer`（sort by expert → 分段跑各 expert → scatter back → ×topk_weight → sum）
- **结构命名**：`model.embed_tokens / layers[i].{self_attn, mlp, input_layernorm, post_attention_layernorm} / norm / lm_head`（不是 transformer.h/ln_1/ln_2/wte/ln_f）
- hidden_size=2048, num_layers=27, num_heads=16, vocab=102400, eos=100001, bos=100000
- 模型 31.4B ~16GB（bf16），单 L20 44GB 够用 → 单卡 TP=1 跑

## 设计：`models/` 适配器架构

### 目录结构
```
models/
  __init__.py          # build_adapter(config) → 按 model_type 分派
  base.py              # ModelAdapter 抽象基类（定义接口 + 共享工具）
  qwen/
    __init__.py
    adapter.py         # QwenAdapter(ModelAdapter)
  deepseek/
    __init__.py
    adapter.py         # DeepSeekAdapter(ModelAdapter)
    moe.py             # MoE 推理 kernel（expert dispatch，CUDA-Graph 友好）
```

### `ModelAdapter` 接口（base.py）
适配器不重写整个 runner，而是把**架构差异**抽成几个钩子，runner 调用钩子。这样 runner 主体（CUDA Graph / prefill 框架 / 调度）保持单一实现。

```python
class ModelAdapter(ABC):
    # —— 元信息 ——
    model_type: str
    def cache_dims(self, cfg) -> tuple: ...        # (n_heads, kv_n_heads, head_size) 给 KVCacheManager
    def intermediate_size(self, cfg, world_size): ...
    def num_layers(self, cfg): ...

    # —— 权重预处理（替代原 prepare）——
    def prepare_weights(self, model, world_size, rank): ...   # 把各 block 权重重排成 runner 用的 _xxx

    # —— 模块访问（统一命名）——
    def embed(self, model): ...                   # → embedding 层
    def blocks(self, model): ...                  # → layer 列表
    def final_norm(self, model): ...
    def lm_head(self, model): ...

    # —— 单层钩子（decode 用）——
    def compute_qkv(self, block, h_normed, buf, bs): ...      # 返回 attention 输入（结构无关的中间态）
    def attention(self, block, attn_input, layer_idx, bs, cache_mgr, block_table, rope_cos, rope_sin): ...
    def compute_ffn(self, block, attn_out, residual, buf, bs, fast_mode): ...

    # —— prefill 单层钩子 ——
    def prefill_layer(self, block, h, layer_idx, B, S, cache_mgr, block_table, rope_cos, rope_sin): ...
```

> 实现策略：先做**最小可行抽象**。QwenAdapter 直接包装现有逻辑（把 model_graph.py 现有代码搬进钩子，行为 1:1 不变）。DeepSeekAdapter 实现真正的新逻辑（MLA + MoE）。runner 改为持有 `self.adapter` 并调用钩子。

### Runner 改造（model_graph.py / model_prefill.py）
- 构造时 `self.adapter = build_adapter(model.config)`，不再硬编码 `model.transformer.h`。
- `prepare()` → 调 `self.adapter.prepare_weights(...)`。
- `decode()` / `prefill forward()` → 用 `adapter.embed/blocks/final_norm/lm_head` + 逐层 `adapter.prefill_layer` 或 `compute_qkv→attention→compute_ffn`。
- RoPE：把 `_cos_pool/_sin_pool` 维度做成 adapter 决定（Qwen=full head_size；DeepSeek=qk_rope_head_dim=64）。flash_attn 调用对 DeepSeek 走**自定义 MLA 路径**（见下）。

### KV Cache 对 MLA 的处理（cache_manager.py + adapter）
关键决策：**KVCacheManager 保持通用**（仍存 `[n_blocks, block_size, C, D]`），但对 DeepSeek 存的是压缩 latent：
- `cache_dims` 对 DeepSeek 返回 `n_heads=1, head_size=kv_lora_rank+qk_rope=576`（MQA 风格：1 个 latent head）。
- prefill/decode 时 `kv_a_proj_with_mqa` 产出 `[B,S,576]`，reshape 成 `[B,S,1,576]` 存入 cache（复用现有 `flash_attn_with_kvcache` 的 paged 写入或 `store_kvcache`）。
- attention 时：从 cache 读出 `[1,576]` latent → 拆 `compressed_kv(512)|k_pe(64)` → `kv_a_layernorm` → `kv_b_proj` 展开成 per-head `k_nope[16,128]`、`v[16,128]` → 与 `q_nope|q_pe` 拼接 → 用 `flash_attn_varlen_func` 或 `flash_attn_func` 算注意力。
- RoPE 只在 q_pe/k_pe（64 维）上做，**不能**用 flash 的 `rotary_cos/sin`（那是对整维度的）。所以 DeepSeek MLA 路径：**先在外部用 `_cos_pool/_sin_pool`(64 维) 对 q_pe/k_pe 旋转，再传给 flash_attn_func（不带 rotary 参数）**。

### MoE 推理（deepseek/moe.py）
- decode（bs 个 token，每 token 独立路由）：复用 HF 的 `moe_infer` 思路但张量化——gate 算 `topk_idx/topk_weight` → 按 expert 排序分段 → 对每段跑 expert MLP（gate/up/down SwiGLU）→ scatter 回原序 → ×weight sum → + shared_experts。
- CUDA Graph 兼容：MoE 有 data-dependent 的分段（topk 结果决定哪些 token 给哪个 expert），**无法直接进 CUDA Graph**。方案：decode 路径对 MoE 层**不进 graph 的静态 capture**，而是在 graph replay 之外用 eager 跑 MoE，或对常见 bs 用固定 buffer 的分组算法（所有 64 expert 都跑，用 mask 选择——简单但慢）。**首版采用 eager MoE（正确性优先）**，CUDA Graph 仅 capture attention+dense 部分；若性能不足再优化。先保证跑通。
- 权重预处理：把 64 个 expert 的 gate/up/down 堆叠成 `[64, 2*1408, hidden]` 和 `[64, hidden, 1408]` 的大矩阵，用 `torch.bmm`/index 算，便于批量。

### engine.py 改造
- `_init_config`：改为调 `adapter.cache_dims/intermediate_size/num_layers`，不再硬编码字段名。`head_size` 等改为 adapter 提供的 cache 维度（DeepSeek 用 576 当 cache head_size，但 attention 内部另算）。
- runner 构造多传 `adapter`。
- 其余（scheduler / sampler / api_server / benchmark）**不动**——它们只认 logits 和 token，架构无关。

## 实施步骤
1. **建 `models/` 目录**：base.py 接口 + `__init__.py` 分派（`deepseek_v2`→DeepSeek, 默认/`qwen`→Qwen）。
2. **QwenAdapter**：把 model_graph.py / model_prefill.py 现有 Qwen 逻辑搬进钩子，**保证 1:1 等价**。
3. **改 runner**：用 adapter 钩子替换硬编码，Qwen 跑通回归（用现有 benchmark/curl 验证输出不变）。
4. **DeepSeekAdapter**：
   - 模块访问（embed_tokens/layers/norm/lm_head）
   - MLA attention 钩子（latent cache + 外部 RoPE + flash_attn_func）
   - MoE 钩子（eager moe_infer + shared experts）
   - 权重预处理（堆叠 expert 矩阵）
5. **engine 改 cache_dims** 让 KVCacheManager 对 DeepSeek 存 576 维 latent。
6. **下载完成 + 跑通 DeepSeek-V2-Lite**：curl /generate_stream 验证输出合理；benchmark 测吞吐。
7. **修 generate() 残留 bug**：`scheduler.finished_sequences.clear()`（顺手，与适配独立）。

## 风险与取舍
- **MoE + CUDA Graph**：首版 MoE eager，正确性优先；decode 延迟会高于纯 attention。可接受（用户要"跑起来正常服务"，先正确再优化）。（用户已确认：正确优先 MoE eager）
- **下载**：用户已确认换 modelscope.cn 重新后台下载（更稳），原 hf-mirror 下载停掉。
- **MLA 的 flash_attn 调用**：不能用 `flash_attn_with_kvcache` 的 rotary（维度不符），改用先外部 RoPE 再 `flash_attn_func`。prefill 的 paged 写入 latent 用 `store_kvcache`（1 head × 576）。
- **单卡显存**：31.4B bf16≈16GB 权重 + KV cache（576 维 latent 很省），L20 44GB 充足，TP=1。
- **回归**：Qwen 路径行为必须不变，步骤 3 单独验证后再动 DeepSeek。
- **下载**：hf-mirror 超时频繁，31GB 预计较久；并行写代码，下载好了再联调。
