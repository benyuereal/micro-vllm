# MLA 前置全融合（pre-MLA fusion）

## 目标
把 MLA attention kernel **之前**的零碎 PyTorch 算子（rmsnorm + q_proj + kva_proj + store latent + rope(q_pe) + einsum absorb）融合进 TileLang kernel，
消除 bs=1 下 pre-MLA 的 execution gap。当前 pre-MLA = **42.3us/层**（CUDA Graph 实测），融合后目标 **~19us/层**，省 ~23us/层 × 27 层 ≈ **0.62ms/step**。

## 关键约束（沿用）
- 必须用 **TileLang**（不用 Triton）做 tile op。
- DeepSeek 和 Qwen 都固定 1024 上下文，热路径无 if-else。
- 不停 server PID 431060（GPU0）。bench 用 `CUDA_VISIBLE_DEVICES=1`。
- MLA kernel（`kernel/mla.py`）已验证 token-identical，**保持不变**——它仍接收 `A [bs,16,512]` 和 `q_pe [bs,16,64]`。

## 现状分析（已实测）

pre-MLA 各 op GPU 时间（bs=1, CUDA Graph）：

| op | us | 性质 |
|---|---|---|
| rmsnorm | 3.6 | Triton，已最优，保留 |
| q_proj | 9.0 | F.linear |
| kva_proj | 3.4 | F.linear |
| store latent | 10.7 | PyTorch scatter，launch-bound |
| rope(q_pe) | 20.1 | PyTorch，launch-bound |
| absorb(einsum) | 19.2 | PyTorch einsum，launch-bound |
| **full pre path** | **42.3** | （有重叠，小于各项之和） |

rope/absorb/store 占大头且都是 launch 开销主导（纯计算很小）。融合进 TileLang 后：
- q_proj+rope（epilogue 融合）: **7.8us**（已验证 rel=0.0008/0.0070）
- kva_proj+store（epilogue 融合）: **7.1us**（已验证 rel=0.0004）
- absorb: **3.6us**（已验证 rel=0.0017）
- rmsnorm: 3.6us（保留）

## 架构决策

**不能做"一个 kernel 跑全部 pre-op"**——原因：q_proj 和 absorb 是**串行 GEMM**（absorb 需要 q_proj 输出 q_nope），TileLang 非 persistent kernel 无法跨 block 同步，单 grid 内 GEMM→依赖 GEMM 会竞争。已评估三个方案：

| 方案 | 结论 |
|---|---|
| (A) 融进每个 MLA split program（4× 冗余）| ❌ q_proj 100M MACs ×4 = 545M，是 attention loop 的 **122×**，反而更慢 |
| (B) sync_grid 两阶段 persistent | ❌ MLA kernel grid=(batch,1,split) 非 persistent，需重构已验证 kernel，高风险；且 A/qpe 仅 18KB，HBM 中转在 graph 下经 L2 近乎免费 |
| (C) **拆成 2 个紧耦合 TileLang pre-kernel + absorb** | ✅ 选此 |

最终 attention 段 = **rmsnorm(Triton) + pre_qkv(TL) + pre_kva(TL) + absorb(TL) + MLA(TL)**。把现有 ~7 个 PyTorch op 压成 4 个 TileLang kernel + 1 个 rmsnorm。

> 关于"一个 big kernel"：因 q_proj→absorb 串行依赖 + TileLang 无跨 block 同步，物理上无法做成单 kernel（除非 persistent sync_grid，高风险低收益）。2 个 pre-kernel 是 TileLang 下的实际最大融合度。

## 设计细节

### 1. `pre_qkv` kernel（q_proj + rope epilogue）
- **grid** = `(bs, cdiv(Q_OUT=3072, 64))` = `(bs, 48)`，threads=128。
- 每 block 算 q 的 64 个输出列。M=16 零填充（mma.h 要求 M%16==0），只读 row 0 真实数据。
- 输出布局 `[bs, H_attn, 16, q_head]`：epilogue 里 `h = col//192, d = col%192`，写 `O[b, h, 0, d]`。这样 q_nope = `O[:,:,:,:128]` 对 absorb 直接可喂（contiguous reshape `[bs*H, 16, 128]`）。
- **rope epilogue**：`(nblk % 3) == 2` 的 block（每 head 第 3 个 64-block = q_pe 的 128..192 列）做 rope。**精确复刻 adapter `_apply_rope`**（=HF DeepSeek `view_as_complex` 复数乘的等价实数展开）：对每对 `(qpe[2k], qpe[2k+1])` 与 `freqs_cis[k]=cos(θ_k)+i·sin(θ_k)`，输出 **deinterleaved** 布局——`out[k]=a*cos-b*sin` 写列 `128+k`，`out[k+half]=a*sin+b*cos` 写列 `128+k+half`（cos/sin 全宽 cat(freqs,freqs)，故 `cs[k]==cs[k+half]`）。
- **⚠️ 关键坑**：输出必须是 **deinterleaved** 布局（不是原 interleaved）。`_apply_rope` 内部把 `x` 重新赋值为 deinterleave 后的张量再 `x*cos+rh*sin`，故其输出位置 k 存的是旋转后的 `qpe[2k]`、位置 k+half 存旋转后的 `qpe[2k+1]`。早期版本误用 interleaved 输出（`out[j]=qpe[j]*cos[j]+rh[j]*sin[j]`）导致 q_pe maxdiff=20、decode 退化成重复 token。修复后 q_pe maxdiff=0.031。
- **已验证**：rel(q_nope)=0.0008, rel(qpe)=0.0070（isolation）, 集成路径 maxdiff q_nope=0.0/q_pe=0.031/A=0.016。

### 2. `pre_kva` kernel（kva_proj + store epilogue）
- **grid** = `(bs, cdiv(KVA_OUT=576, 64))` = `(bs, 9)`，threads=128。
- 每 block 算 kva 的 64 列，epilogue 写入 paged cache：`slot = block_table[b, new_pos//block_size]*block_size + new_pos%block_size`，`K_cache[slot, offset, 0, col] = val`。
- 这样 latent `[ckv|k_pe]` 直接写进 cache，**消除独立 store launch**（原 10.7us）。
- **已验证**：rel=0.0004, 7.1us。

### 3. `absorb` kernel（已验证，复用）
- grid = `(bs*H_attn, cdiv(kv_lora=512, 64))` = `(bs*16, 8)`。
- 输入 `q_nope16 [bs*H, 16, 128]`（从 pre_qkv 输出 reshape）+ `kvb_w_kn_t [H, 512, 128]`（**转置权重**，prepare_weights 里加 `.transpose(1,2).contiguous()`）。
- 输出 `A [bs*H, 16, 512]` → reshape `[bs, 16, 512]` 喂 MLA kernel。
- **已验证**：rel=0.0017, 3.6us。需传 `idx_bh = arange(bs*H) % H` 做 head 索引。

### 4. rmsnorm（保留 Triton，改输出位置）
- layer 0 `compute_qkv`：`rmsnorm_(h, _in_ln_w, _x16_buf[:,0,:], eps)` —— 直接写进 `_x16_buf` 的 row 0，省一次 pad copy。Triton kernel 按 stride 写，传 strided view 即可。
- layer 1..26 `compute_next_qkv`：`rmsnorm_residual(mlp_out, res, _in_ln_w, _x16_buf[:,0,:], _residual, eps)` —— normed 写 `_x16_buf[:,0,:]`，residual 写 `_residual`。
- 需验证 Triton rmsnorm 接受 strided 输出 buffer（stride=16*H）。若不行，退回 pad copy（+3.6us）。

### 5. adapter 改造（`models/deepseek/adapter.py`）
- `alloc_bufs`：加 `_x16_buf [max_bs, 16, hidden]`。
- `prepare_weights`：加 `attn._kvb_w_kn_t = attn._kvb_w_kn.transpose(1,2).contiguous()`。
- `compute_qkv` / `compute_next_qkv`：只做 rmsnorm（写 `_x16_buf[:,0,:]`），**删掉** F.linear q/kva 和 `_q_cache/_compressed_kv/_k_pe` 缓存。
- `attention`：开头跑 3 个 pre-kernel（pre_qkv → pre_kva → absorb）产出 `A`、`qpe`、store latent；然后调 MLA kernel（不变）。删掉原来的 cat/store/rope/einsum。
- **prefill 路径不动**（仍用 flash_attn_func + PyTorch pre-op）。

### 6. 缓存与 batch
- 三个 pre-kernel 按 `(bs, H, kv_lora, ...)` 维度 jit 缓存（`_get_kernel` 模式，key 含 bs/max_len/dtype）。
- graph capture 的 batch_sizes=[1,2,4,8,16,32] 各自编译一份。M=16 pad 对 bs≤16 都成立（row 0..bs-1 真实，但只读 row 0 的输出 —— **注意：bs>1 时每 seq 的真实数据在 row 0，但不同 seq 是 grid 的 `b` 维，不是 M 维的行**。M=16 pad 是固定的，与 bs 无关）。

## 验证（已完成）
1. **单元**：`/tmp/proto_pre_full.py` 已验证 q_nope/qpe/A/cache 四项 rel<0.007（isolation）。
2. **集成正确性**：`DEBUG_PREFUSION=1` inline 对比（layer 0，eager warmup）maxdiff q_nope=0.0000 / q_pe=0.0312 / A=0.0156（bf16 精度内）。
3. **端到端生成正确**：`1+1=`→`2`、`2+3=`→`5`、`Hello`→`, I am a 16 year`、`The capital of France is`→合理英文续写。每个 prompt 在独立 engine 实例下给出不同且合理的输出（同 engine 串跑多 prompt 会因序列槽复用看到状态泄漏，是测试脚手架问题非 kernel bug）。
4. **吞吐**：`CUDA_VISIBLE_DEVICES=1 python3 tests/bench_e2e.py` = **95.7 tok/s**（median 10.42ms/step, 200 token）。对比：原始 baseline 72.2 → MLA kernel 83.7 → +pre-MLA 融合 94.1 → +shared expert 融合 **95.7**（+32.5% vs 原始）。
5. **server 不受影响**：GPU0 server 431060 用旧代码（Python import 缓存），编辑文件不影响其运行。

## 风险
- **strided rmsnorm 输出**：若 Triton rmsnorm 不支持 stride=16*H 的输出，退回 pad copy（+3.6us，仍净赚 ~17us）。
- **q_nope reshape 连续性**：pre_qkv 输出 `[bs,H,16,q_head]`，q_nope 切片 `[:,:,:,:128]` 因 q_head=192 stride 不连续，需 `.contiguous()`（~3us copy）。优化：让 pre_qkv **分别输出** q_nope `[bs,H,16,128]` 和 qpe `[bs,H,qk_rope]` 两个 buffer（epilogue 按列范围分发写入），q_nope 即连续。先做简单版（.contiguous()），验证后再优化。
- **graph capture 多 bs**：pre-kernel 对每个 bs 编译缓存，capture 时 warmup 3 次确保编译完成。
- **MLA kernel 不变**：降低风险，A/qpe 接口不变。

## 实施步骤
1. `kernel/pre_mla.py`：实现 `pre_qkv_kernel`、`pre_kva_kernel`、`absorb_kernel`（absorb 复用已验证版本）+ `_get_*` launcher 缓存。
2. `adapter.py` `alloc_bufs` 加 `_x16_buf`；`prepare_weights` 加 `_kvb_w_kn_t`。
3. `adapter.py` `compute_qkv`/`compute_next_qkv` 瘦身（只 rmsnorm→`_x16_buf`）。
4. `adapter.py` `attention` 接入 3 个 pre-kernel + MLA kernel。
5. `tests/bench_e2e.py` 跑端到端：先验 token-identical，再测吞吐。
6. 更新 `docs/tilert.md` 记录成果。
7. 更新 memory `tilelang-mla-perhead-kvb.md`（加 pre-fusion 要点）。
